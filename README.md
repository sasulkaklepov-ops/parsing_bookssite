First, we import the necessary libraries. 
We write the URL of the website. We get its HTML code and convert it to text. 
We use bs4 to work with HTML tags and extract the necessary information. 
We use openpyxl to save the data in an EXCEL spreadsheet.
We work within a class that contains all the necessary information. 
We retrieve the name of the book and its price. We save the data in a spreadsheet.
